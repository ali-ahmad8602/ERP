# ─── InvoiceMate ERP Frontend ─────────────────────────────────────────────────
# Copy this file to .env.local and fill in the values.

# Backend API URL — must include the protocol and port, no trailing slash.
# The backend serves REST endpoints at /api/* and Socket.io on the same origin.
NEXT_PUBLIC_API_URL=http://localhost:5000
